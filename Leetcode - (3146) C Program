int findPermutationDifference(char* s, char* t) {
    int i=0;
    int j=0;
    int diff=0;
    while(s[i]!='\0' && t[j]!='\0'){
        if(s[i]==t[j]){
           diff+=abs(i-j);
           j=0;
           i++;
        }
        else{
            j++;
        }
    }
    return diff;
}
