#include <stdio.h>
#include <string.h>

int main()
{
   int n;
   scanf("%d",&n);
   char names[n][105];
   for(int i=0;i<n;i++){
       scanf("%s",names[i]);
   }
 
   for(int i=0;i<n;i++){
         int repeated=0;
         for(int j=0;j<i;j++){
             if(strcmp(names[i],names[j])==0){
                 repeated=1;
             }
         }
         if(repeated)
            printf("YES\n");
        else
            printf("NO\n");
   }
   
}
