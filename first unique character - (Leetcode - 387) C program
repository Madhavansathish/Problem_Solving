int firstUniqChar(char* s) {
    int freq[128]={0};
    int n=strlen(s);
    int i;
    for(i=0;i<n;i++){
        freq[s[i]-'a']++;
    }

    for(i=0;i<n;i++){
        if(freq[s[i]-'a']==1)
           return i;  
    }
    return -1;
}
